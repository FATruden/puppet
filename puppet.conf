[main]
    logdir          = /var/log/puppet
    rundir          = /var/run/puppet
    ssldir          = $vardir/ssl
    dns_alt_names   = adm.ruden.org,adm
    hiera_config    = /etc/puppet/hiera.yaml

[agent]
    classfile       = $vardir/classes.txt
    localconfig     = $vardir/localconfig
    server          = adm.ruden.local
